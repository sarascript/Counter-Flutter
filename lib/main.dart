import 'package:flutter/material.dart';
import 'src/app.dart';

// La app buscará el main del main.dart para empezar a ejecutarse
void main() {
  // Método que ejecuta la app
  runApp(MyApp()); // Necesita un widget para cargar algo
}